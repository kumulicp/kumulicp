name: LDAP Account Manager Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  ldap-tests:

    runs-on: ubuntu-latest

    services:
      ldap:
        image: osixia/openldap:1.5.0
        ports:
          - 389:389
        env:
          LDAP_ORGANISATION: Demo
          LDAP_DOMAIN: example.com
          LDAP_ADMIN_PASSWORD: admin
        options: >-
          --health-cmd "ldapsearch -x -H ldap://localhost -b dc=example,dc=com -D 'cn=admin,dc=example,dc=com' -w admin"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: shivammathur/setup-php@15c43e89cdef867065b0213be354c2841860869e
      with:
        php-version: '8.4'
    - uses: actions/checkout@v4
    - name: Copy .env
      run: php -r "file_exists('.env') || copy('.env.example', '.env');"
    - name: Install Dependencies
      run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
    - name: Generate key
      run: php artisan key:generate
    - name: Directory Permissions
      run: |
        mkdir -p storage/framework/views storage/framework/sessions storage/framework/cache storage/app/purifier
        chmod -R 777 storage bootstrap/cache
    - name: Create Database
      run: |
        mkdir -p database
        touch database/database.sqlite
    - name: Execute AccountManager tests against both drivers
      env:
        DB_CONNECTION: sqlite
        DB_DATABASE: database/database.sqlite
        ACCOUNTMANAGER_DRIVER: ldap
        LDAP_HOST: localhost
        LDAP_PORT: 389
        LDAP_USERNAME: "cn=admin,dc=example,dc=com"
        LDAP_PASSWORD: admin
        LDAP_BASE_DN: "dc=example,dc=com"
      run: php artisan test --testsuite=AccountManagerLdap
